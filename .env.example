# Gemini API key (server-side only — never prefix with NEXT_PUBLIC_)
# Get yours from https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Model ID — must support JSON mode (responseMimeType + responseSchema)
GEMINI_MODEL=gemini-3.5-flash-lite
