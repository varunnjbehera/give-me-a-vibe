import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Give Me a Vibe — Give me something to build",
  description:
    "Press one giant button. Get one brutally honest product idea worth vibe-coding. No inputs, no accounts, no mercy.",
  keywords: ["vibe coding", "startup ideas", "side projects", "build something"],
  openGraph: {
    title: "Give Me a Vibe",
    description: "Give me something to build.",
    type: "website",
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}>
      <body className="flex min-h-full flex-col">{children}</body>
    </html>
  );
}
